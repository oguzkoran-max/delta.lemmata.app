# P004: Exact Family Palette and Language Weave

## Role

Act as a senior product designer, accessibility specialist, digital-humanities
methodologist, and Streamlit engineer. Refine the accepted beginner-first Delta
workbench without adding scientific analysis, runtime AI, or non-working controls.

## User Request

Use the actual colors from `lemmata.app` and `lda.lemmata.app`, not merely a
similar palette, and make the entry composition more creative while preserving
the Lemmata product family.

## Dated Comparative Source

Inspect both live sibling products on 2026-07-12. Record computed styles and CSS
custom properties rather than estimating colors from screenshots. Treat the live
pages as dated comparative sources; they may change later.

Use these exact Lemmata tokens:

- teal `#0f6e56`, dark teal `#0a5443`;
- teal surfaces `#e8f5f0`, `#c5e8dc`, and accent `#5dcaa5`;
- family canvas `#f8faf9`, text `#1a1a1a`, secondary text `#5c5c5c`, tertiary
  text `#888888`, and border `#e2e5e4`;
- coral `#d85a30` / `#fef0eb`;
- amber `#b8860b` / `#fdf6e3`;
- blue `#185fa5` / `#e8f0fa`;
- purple `#7c3aed` / `#f3eefe`.

Retain the exact LDA workbench canvas `#f8f9fa`, sidebar `#f0f2f6`, control text
`#31333f`, and primary `#0f6e56` where Streamlit owns the native controls.

## Creative Direction

Replace the faint decorative function-word line with a full-width, code-native
`Language Weave` teaching figure. It must:

- show two explicitly illustrative text rows;
- use common words only as a conceptual example, without counts or axes;
- identify common words, punctuation, sentence rhythm, and vocabulary as signal
  families;
- retain the Observe, Compare, Interpret method path;
- state that it is an illustration and no corpus has been analysed;
- never resemble a dendrogram, heatmap, cluster, distance score, authorship
  fingerprint, or computed result.

The hero is a full-width teaching band, not a card or split marketing layout.
Use no gradients, fake metrics, decorative blobs, or nested cards. Keep the actual
research-purpose control and Upload workflow in or immediately adjacent to the
first viewport.

## Responsive Contract

- Audit 1440x1000, 1280x800, 640x800, 390x844, 360x800, and 320x800.
- The purpose choices must begin inside every initial viewport.
- The desktop 1280x800 viewport must contain the next work-surface hint.
- Mobile must preserve definition, corpus-relative boundary, illustration label,
  signal key, and method order without horizontal overflow or word fragments.
- Color is never the only carrier of meaning; every swatch has a text label.
- Screen-reader descriptions may be visually compacted but must remain in the
  accessibility tree.

## Verification

1. Lock every exact family token in the browser audit.
2. Assert the Language Weave structure, labels, two rows, four signal types, and
   absence of the old decorative pattern.
3. Preserve TXT and two-member ZIP Upload-Describe-Review regressions, payload
   absence, no-egress, clean-console, keyboard, download, and Review checks.
4. Run the full repository gate at 100% measured statement and branch coverage.
5. Retain every failed browser JSON. Keep the full screenshot set only for the
   passing candidate to avoid redundant binary repository growth.
6. Disclose agent budget overruns and do not count those reports as independent
   approval.

## Non-Goals

- No R `stylo` execution, parameter control, result, stability label, AI, account,
  storage, deployment, parent-site edit, or scientific claim.
- No claim of general usability, Safari compatibility, VoiceOver conformance, or
  P004 human acceptance.
