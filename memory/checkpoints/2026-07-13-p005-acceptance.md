# P005 Acceptance Checkpoint

**Date:** 2026-07-13

## Current State

- Branch: `codex/p005-job-lifecycle`
- Remote implementation head: `2a17ec60ed62695e1e47383ad930330bef52f134`
- Local evidence head: clean worktree, one evidence commit ahead, not pushed
- Ticket: `provenance/tickets/P005.json`, `in-progress`
- Acceptance: AC-01--AC-07 passed; AC-08 pending
- Public analysis: still locked; real R/stylo work belongs to P006/P008

## Verified

- `RUN-20260713-0003` rebuilt final implementation commit in fresh
  `--no-hardlinks` clones.
- 950 tests, 6,551 statements, 1,732 branches, and 100% measured coverage passed.
- Repository, metadata, schema, record, and locked R boundaries passed.
- P004 public browser boundary and all 16 P005 lifecycle projections passed from
  clean exact-commit states with `git_dirty: false`.
- GitHub run `29220278021` passed final-commit Linux verification, supply-chain
  generation, and canonical Linux amd64 container build.

## Historical External Gate

`actions/upload-artifact` rejected the generated package because account artifact
storage was full. Seven already-expired Windows build artifacts in
`dtcf-sinav-programi-planlayici` (about 2.5 GB total) were deleted. GitHub documents
a 6-12 hour storage-usage recalculation delay. The immediate retry, a third attempt
about 20 minutes later, and the fourth attempt at 12:55 TRT all passed every
preceding gate and failed only at upload. The fourth retry was verify job
`86786405954`; the action found all eight expected files before the quota rejection.
The quota failure is not bypassed with `continue-on-error`; it remains retained
failure evidence. The retry instruction below was superseded by
`HD-20260713-0001` before another rerun.

## Superseded Retry Steps

1. Confirm local time is at least 2026-07-13 18:55 TRT, the conservative end of
   GitHub's stated recalculation window.
2. Run `gh run rerun 29220278021 --failed`.
3. Wait for the `verify` job and confirm artifact upload succeeds.
4. Download `supply-chain-2a17ec60ed62695e1e47383ad930330bef52f134`.
5. Verify its internal `checksums.sha256` and inspect SBOM/audit files.
6. Update `provenance/evidence/P005/final-ci-validation.md`, mark P005-AC-08 passed,
   clear the blocker, rerun `./scripts/verify.sh`, commit, and push.
7. Do not record product-owner acceptance; the shared walkthrough remains P015.

## Closure Result

- Exact capture source: `d3ca0f67a6a08f2e6a232b0b686b36842ef819e9`
- Linux capture run/job: `29268150070` / `86878774447`, success
- Normal source CI verify/container: `29268150409`, success
- Git-backed evidence commit: `2eff470e4e06a1b1586c4aec251ba99162770dbc`
- Run record: `RUN-20260713-0004`
- Temporary write workflow removal commit: `029248b07b6952e9bccdca88ae46cf7cc51c050b`
- Closure CI verify/container: `29269051028`, jobs `86881820484` and
  `86881820512`, success
- P005 acceptance: AC-01 through AC-08 passed; no blockers

The shared product-owner walkthrough remains P015. This closure does not add or
verify scientific stylo computation, production isolation, secure erase, or
deployment.

## Current Exact Steps After HD-20260713-0001

1. Commit the path-neutral generator, structural validator, focused tests, and
   branch-scoped temporary Ubuntu capture workflow.
2. Push the same exact source to `codex/p005-evidence-capture`.
3. Verify regular Linux CI and the capture run, then fetch the bot commit.
4. Validate the eight files, nested source checksums, outer SHA-256 manifest, JSON
   structures, and absence of private path markers.
5. Bind the exact source, workflow run/job, captured commit, and package hashes in a
   new Run record with reciprocal P005 links.
6. Remove the temporary write-capable workflow, run the full gate, and require the
   normal final GitHub CI to pass before marking AC-08 and P005 complete.
7. Do not record product-owner acceptance; the shared walkthrough remains P015.
