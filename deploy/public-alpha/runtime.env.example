# Generate the real root-owned 0600 file with scripts/generate_p014_secrets.py.
# Never reuse a value, copy a Lemmata secret, or commit the generated file.
DELTA_JOB_OWNER_SECRET_HEX=<independent-64-lowercase-hex-characters>
DELTA_PREPARATION_AUTHORITY_SECRET_HEX=<different-64-lowercase-hex-characters>
DELTA_RECOVERY_RECEIPT_SECRET_HEX=<third-different-64-lowercase-hex-characters>
