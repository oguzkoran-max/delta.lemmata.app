# Failed Browser Audit Run 1

The first tracked P004 browser audit produced all six initial viewport screenshots,
then timed out while waiting for purpose-guidance copy that remained inside a closed
expander. The keyboard assertion was subsequently bound to the segmented control's
`aria-checked` state. Because the process exited before result assembly, this directory
has no `browser-audit.json` and is retained only as failed-attempt evidence.
