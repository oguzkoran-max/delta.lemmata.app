# P014 Phase B Independent Review Remediation

## Supersession Boundary

This addendum preserves `phase-b-visual-integration-validation.md` byte-for-byte
because `RUN-20260717-0001` already binds that historical artifact by SHA-256.
It corrects later-disproved statements without rewriting the earlier Run.

- A5.1 was owner-selected, not scientifically or operationally approved.
- The earlier three-GO summary lacked durable reviewer artifacts and is
  withdrawn.
- The complete 24-file A5.1 package, original manifest, transcribed Claude
  report, and source boundary are now retained in Git under
  `provenance/evidence/P014/phase-a51-design-source/`.
- The Phase B remediation tree is frozen and independently re-reviewed at exact
  implementation commit
  `d637893a19cc33e57b8826c5ff8625bd196cb1d4`.
- The branch remains a draft pull request. Phase B code/evidence closure is not
  merge authorization, deployment approval, public readiness, or P014 ticket
  closure.

## Remediation Scope

The current P014 follow-up addresses the independently reported findings without
changing the fixed Guided scientific grid:

- immediate and periodic terminal-job maintenance independent of browser traffic;
- explicit exploratory, rather than unavailable, Style Over Time readiness;
- purpose-conditioned chronology guidance and warning-only warning counts;
- expanded parameter explanations placed before confirmation and run controls;
- uploader purpose/mode information included in accessible names;
- durable, checksum-verifiable A5.1 design-source retention;
- one machine-readable active ticket, with P007-P009 deferred as `blocked`;
- extractable content-free browser JSON and SHA-256 markers in canonical CI.

## Retained Exact-Commit Sequence

The final result does not replace the failed or conditional evidence:

1. `7585d83a45dbc35580fc85346c2bdc731c07c720` exposed premature WORK
   cleanup between scientific completion and public result finalization.
2. `8198dd82a30af2f6c89301ab38189e1b1b0b4fe9` retained a stale
   `finalizing` presentation after the scientific result and export existed.
3. `a5b94d5ffe36d18e6d16399ef0c8be7cb671dbdb` reached a correct
   scientific lifecycle but failed the browser evidence aggregator with a
   result-surface `KeyError`.
4. `dfce0299ce5674d2732870c6e286a5b6419e27aa` passed automation but
   independent review remained conditional or NO-GO because cross-owner
   predecessor propagation, cumulative worker/gateway timing, terminal restart,
   queue status, computed-font, and table-region evidence were unresolved.
5. `8c813ff33f735172fb6d7e16e6eb76c1927a4d02` closed those product and
   lifecycle findings. Its push run `29591314260` retained one browser-harness
   result-selection failure while its parallel PR run `29591317281` passed.
6. `d637893a19cc33e57b8826c5ff8625bd196cb1d4` added one bounded,
   observable result-selection fallback without changing application behavior,
   parameters, scientific values, or exports. Exact push run `29592151976` and
   pull-request run `29592158057` both passed verify and container jobs.

The corresponding content-free artifacts are retained under
`provenance/evidence/P014/phase-b-exact-*/`.

## Exact Closure Evidence

At exact commit `d637893a19cc33e57b8826c5ff8625bd196cb1d4`:

- canonical Linux passed 1,726 tests, 11,692 statements, and 3,050 branches at
  100% measured coverage;
- verify jobs `87923605718` and `87923626171` passed;
- hardened-container jobs `87923605615` and `87923628578` passed;
- browser evidence reported a clean Git state, canonical worker platform,
  passed FIFO, viewport, terminal cleanup, semantic result, chart, table, and
  export gates;
- an unrelated owner's predecessor job terminalized as `crashed`, exposed no
  result/export, and had input, work, result, and export artifacts verified
  absent;
- the expected user's job terminalized as `succeeded`, exposed its confirmed
  scientific result/export, and had private input/work artifacts verified
  absent;
- native-keyboard result selection succeeded; the bounded fallback was retained
  as tested recovery behavior but was not needed in the passing push audit;
- retained browser JSON SHA-256 is
  `e2508e6152abd7a639323c6af47d69b29dae7affad3973709b645c65fc911578`;
- canonical export SHA-256 is
  `5935e60c3deef20779174b81e652078c5762ccecece7945351e2615ff6072f17`.

Three independent exact-SHA reviews found no actionable P0-P3 issue and returned
`GO` only for Phase B code and automated evidence closure. Their durable
verdicts are under
`provenance/evidence/P014/phase-b-exact-d637893/independent-reviews/`.

## Open Gates

P014 remains `in-progress`. AC-08 through AC-10 still require accepted target-host
inventory, live coexistence/load evidence, restart cleanup, rollback, integrated
owner walkthrough, and an explicit Oğuz Koran activation decision. AC-05 remains
pending at ticket level until its complete deployment-profile hostile,
saturation, timeout, cancellation, and cleanup criterion is evidenced as one
accepted gate.

P012's complete FAIR package, benchmark accuracy, manual screen-reader testing,
general usability, literary findings, and publication readiness remain outside
this closure. This addendum does not authorize merge, image publication, VPS,
Caddy, DNS, or public activation.
